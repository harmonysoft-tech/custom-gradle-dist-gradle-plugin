apply plugin: 'java'

group 'org.my'

$repository$

jar {
    manifest {
        attributes(
                'Implementation-Version': "$version",
                'Built-Date': new Date().format("dd-MM-yyyy")
        )
    }
}