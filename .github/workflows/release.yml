name: Java CI with Gradle

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    uses: harmonysoft-tech/ci/.github/workflows/gradle-plugin.yml@main
    secrets: inherit
    permissions:
      contents: write